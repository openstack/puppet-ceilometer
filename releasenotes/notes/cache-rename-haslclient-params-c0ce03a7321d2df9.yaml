---
deprecations:
  - |
    The ``ceilometer::cache::hashclient_retry_delay`` parameter was deprecated
    in favor of the new ``hashclient_retry_timeout`` parameter.

  - |
    The ``ceilometer::cache::dead_timeout`` parameter was deprecated in favor
    of the new ``hashclient_dead_timeout`` parameter.
